# Netlify configuration for Next.js (SSR + API Routes)
# The @netlify/plugin-nextjs plugin handles serverless functions automatically

[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"
